---
interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<article
  class={`max-w-lg rounded-md md:border-2 md:border-r-8 md:border-b-8 border-supa-white text-slate-50
  p-8 md:px-8 md:py-12 text-justify relative ${className}`}
>
  <slot />
</article>
